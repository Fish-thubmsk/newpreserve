# .env.example
# 复制此文件为 .env，并填入真实的 Cookie 值。
# 注意：.env 文件已加入 .gitignore，不会被提交到 Git 仓库。

# -----------------------------------------------
# 如何获取 Cookie：
# 1. 使用手机或电脑浏览器打开超星图书馆预约页面并登录
#    （通常通过微信或超星学习通跳转至 office.chaoxing.com）
# 2. 打开 Chrome DevTools（F12）→ Network 面板
# 3. 刷新页面，找到任意一条对 office.chaoxing.com 的请求
# 4. 在 Request Headers 中找到 Cookie 字段，复制完整的值
# 5. 粘贴到下方 CX_COOKIE 引号内
# -----------------------------------------------

# Cookie 字符串（直接从浏览器 DevTools 中拷贝）
CX_COOKIE="UID=xxx; JSESSIONID=xxx; fid=xxx; _uid=xxx; ..."
